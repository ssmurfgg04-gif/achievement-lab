# Day 006 — setdefault()

Today I reviewed **setdefault()**.

Key takeaway: initialize nested dict keys safely.

This note lands via a pull request so the history stays reviewable, one day per branch.
