# Day 028 — datetime.isoformat()

Today I reviewed **datetime.isoformat()**.

Key takeaway: unambiguous timestamps.

This note lands via a pull request so the history stays reviewable, one day per branch.
