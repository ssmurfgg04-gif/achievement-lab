# Day 030 — list.sort(key=lambda)

Today I reviewed **list.sort(key=lambda)**.

Key takeaway: inline sort keys.

This note lands via a pull request so the history stays reviewable, one day per branch.
