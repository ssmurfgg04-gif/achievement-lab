# Day 039 — multiple return values

Today I reviewed **multiple return values**.

Key takeaway: tuple unpacking idiom.

This note lands via a pull request so the history stays reviewable, one day per branch.
