# Day 037 — bool is a subclass of int

Today I reviewed **bool is a subclass of int**.

Key takeaway: True == 1 in Python.

This note lands via a pull request so the history stays reviewable, one day per branch.
