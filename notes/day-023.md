# Day 023 — functools.lru_cache

Today I reviewed **functools.lru_cache**.

Key takeaway: memoize expensive calls.

This note lands via a pull request so the history stays reviewable, one day per branch.
