# Day 026 — any() and all()

Today I reviewed **any() and all()**.

Key takeaway: boolean aggregation over iterables.

This note lands via a pull request so the history stays reviewable, one day per branch.
