# Day 022 — str.split(maxsplit)

Today I reviewed **str.split(maxsplit)**.

Key takeaway: parse strings with control.

This note lands via a pull request so the history stays reviewable, one day per branch.
