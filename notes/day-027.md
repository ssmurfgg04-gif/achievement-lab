# Day 027 — round() vs f-string format

Today I reviewed **round() vs f-string format**.

Key takeaway: display vs precision.

This note lands via a pull request so the history stays reviewable, one day per branch.
